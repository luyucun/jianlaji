# 开发记录

记录每次开发/文档改动的核心结果，方便回滚和新对话接手。不写过程流水账，只写结论。

新记录写在最上面（倒序），最新的最容易被看到。

记录模板：

```md
## YYYY-MM-DD 简短标题
- 改了什么：一句话
- 影响文件：列出主要文件
- 验证情况：验证过什么 / 未验证的原因
- Git 提交：`hash 提交说明`（未提交则写"未提交"）
```

---

## 2026-07-07 修复：StarterPlayerScripts 也会多套一层同名子文件夹，之前漏算了

- 改了什么：用户按上一条修复后的文档指导，把 `home/StarterPlayerScripts` 改成 `home/StarterPlayer/StarterPlayerScripts` 重新做了 Sync to，这次不再报层次结构错误，但用户回来问"路径是不是不对"——检查发现 Studio 在这个目标目录下又自动多生成了一层同名子文件夹 `StarterPlayerScripts`（跟 `ServerScriptService`/`ReplicatedStorage/Modules` 上验证过的"自动多套一层"规律一致，但上一条记录写文档时漏考虑了这条规律同样适用于刚修正的 `StarterPlayer/StarterPlayerScripts` 路径），导致新生成的文件夹是空的，而 `EntranceLeaveController.client.luau` 还留在上一层没被 Studio 接管。把文件挪到 Studio 真正同步的目标路径 `home/StarterPlayer/StarterPlayerScripts/StarterPlayerScripts/EntranceLeaveController.client.luau`，同时把 `battle/` 下尚未绑定过的同名目录也预防性改成一致的三层结构，避免以后配置对局 Place 时再踩同一个坑。《基础技术框架.md》7.2/7.3节路径示例和操作步骤已更新为正确的三层路径。
- 影响文件：`home/StarterPlayer/StarterPlayerScripts/StarterPlayerScripts/EntranceLeaveController.client.luau`（从 `home/StarterPlayer/StarterPlayerScripts/` 挪过来，内容不变）、`battle/StarterPlayer/StarterPlayerScripts/StarterPlayerScripts/.gitkeep`（同上，预防性修正）、`项目设定相关/基础技术框架.md`（7.2/7.3节路径示例修正）
- 遗留问题（下次接手前需要做）：
  1. 需要用户回到 Studio 确认这次 Explorer 里的 `StarterPlayerScripts` 下确实能看到 `EntranceLeaveController` 这个 LocalScript，本地文件挪动后是否被 Studio 正确识别还没验证。
  2. V1.1.1 的完整 Playtest 验证仍然完全没做，等确认脚本被正确识别后才能开始。
- 验证情况：本地目录结构和文件挪动已用 `find`/`git status` 确认；`lua -e "loadfile(...)"` 语法检查通过；Studio 那边是否真的识别到这个脚本，未经实测。
- Git 提交：未提交

---

## 2026-07-07 修复：StarterPlayerScripts 路径命名错误导致 Sync to 报错，本地文件被冲突弹窗覆盖删除

- 改了什么：用户按上一条记录的文档指导，给家园 Place 的 `StarterPlayerScripts` 配置 Script Sync，选择本地目标目录为 `home/StarterPlayerScripts`，Studio 报错"所选路径 'D:/RobloxGame/jianlaji/home/StarterPlayerScripts/StarterPlayerScripts' 与层次结构 'StarterPlayer.StarterPlayerScripts' 不匹配"，随后弹出冲突提示（保留 Studio 版本还是本地版本），用户选了 Studio 版本，导致本地的 `EntranceLeaveController.client.luau` 被覆盖删除。根因是我在上一条记录里文档写错了：`StarterPlayerScripts` 在 Roblox 里不是独立顶层 Service，而是嵌套在 `StarterPlayer` 服务下面（真实路径 `StarterPlayer.StarterPlayerScripts`），跟 `ServerScriptService`/`ReplicatedStorage` 这种自身就是顶层 Service 的情况不同，不能照搬同一套"目标目录名等于实例名"的模式，Script Sync 会校验目标路径是否匹配真实层级链，不匹配就拒绝并弹冲突。文件本身没有真的丢，从 git 历史（`874b1b3`）里用 `git show` 找回内容，恢复到修正后的路径 `home/StarterPlayer/StarterPlayerScripts/EntranceLeaveController.client.luau`。同时把 `battle/StarterPlayerScripts/StarterPlayerScripts/`（尚未在 Studio 里绑定过，还没触发同样的错，属于预防性修正）也改成了 `battle/StarterPlayer/StarterPlayerScripts/`，避免后续给对局 Place 配置时踩同一个坑。《基础技术框架.md》7.2/7.3节补充了这一条命名规则和踩坑说明。
- 影响文件：`home/StarterPlayer/StarterPlayerScripts/EntranceLeaveController.client.luau`（从 `home/StarterPlayerScripts/StarterPlayerScripts/` 迁移，内容不变，从 git 历史恢复）、`battle/StarterPlayer/StarterPlayerScripts/`（目录迁移，同上）、`项目设定相关/基础技术框架.md`（7.2/7.3节补充说明）
- 遗留问题（下次接手前需要做）：
  1. 用户需要重新在 Studio 里对家园 Place 的 `StarterPlayerScripts` 做一次 Sync to，这次目标目录选 `home/StarterPlayer/StarterPlayerScripts`（不是 `home/StarterPlayerScripts`），确认不再报错，且能看到本地的 `EntranceLeaveController.client.luau` 被正确识别成 LocalScript。
  2. V1.1.1 的完整 Playtest 验证（多入口独立占用/挡板/UI联动）仍然完全没有做，这一步做完 Sync to 之后才能开始。
- 验证情况：Roblox 层级结构（`StarterPlayer.StarterPlayerScripts`）和报错文案已用网页搜索交叉确认多个 DevForum 帖子（[Studio Script Sync Beta 帖](https://devforum.roblox.com/t/studio-script-sync-now-in-beta/4065468?page=7)有完全一致的报错案例）；本地文件恢复和目录迁移已用 `find`/`git status` 确认；Studio 里重新绑定是否真的不报错，未经实测。
- Git 提交：未提交

## 2026-07-07 落地 V1.1.1 场景切换细节：多入口独立占用/挡板/UI联动

- 改了什么：用户在 Studio 里手动复制了多份同名 `Entrance_Solo`，并在《策划案.md》补充 V1.1.1 六点细节需求：(1) 支持多个同名入口各自独立占用传送 (2) `Tips.State.TextLabel` 按占用状态显示 `0/1`/`1/1` 并变色 (3) 占用期间用隔离墙挡住其他玩家进入 (4) 倒计时中随时可离开区域解除占用 (5) 传送完成后释放入口供下一人使用 (6) 倒计时期间显示 `StarterGui.Main.LeaveButton`，点击强制离场。
  - 重写 `RoundEntranceService.server.luau`：不再按路径查找唯一入口，改为遍历 `workspace.Base.RoundEntrances` 下所有 `Name == "Entrance_Solo"` 的 Part，每个入口用自身 Instance 作 key 维护独立状态表（`occupant`/`startTime`/`walls`/`tipsLabel`/`idleColor`）。用 `PhysicsService` 注册 `EntranceWall`/`EntranceOccupant` 两个碰撞组并设为互不碰撞：占用发生时在入口四周动态生成4块不可见（Transparency=1）挡板 Part（CollisionGroup=`EntranceWall`），同时把占用者角色所有 BasePart 的 CollisionGroup 设为 `EntranceOccupant`，让占用者能自由出入但挡住其他玩家；释放时销毁挡板、还原碰撞组为 `Default`。主循环每帧先检查已占用入口（占用者是否还在区域内/倒计时是否到点），再检查空闲入口是否有新玩家进入。用代码在 `ReplicatedStorage/Remotes/Home` 下自动创建（而非要求 Studio 手动建）两个 RemoteEvent：`EntranceCountdownSync`（服务端→客户端，通知倒计时开始/结束及剩余时长）、`RequestLeaveEntrance`（客户端→服务端，玩家点击 Leave 后请求强制离场，服务端校验发起者确实是当前占用者，然后把角色 `PivotTo` 推出区域，主循环检测到人不在区域内会按第4点自然释放）。
  - 新增客户端脚本 `EntranceLeaveController.client.luau`：监听 `EntranceCountdownSync` 控制 `LeaveButton` 的 Visible 并用本地 `RunService.Heartbeat` 驱动 `CoundownTime` 文本（格式 `xS`），监听 `Leave` 按钮的 `Activated` 触发 `RequestLeaveEntrance`。
  - 《架构与Remote登记.md》新增 Home 系统分类，登记上述两个 Remote，状态直接标"已实现"（先改文档再写代码，符合硬规则2）。
- 影响文件：`home/ServerScriptService/ServerScriptService/Home/RoundEntranceService.server.luau`（重写）、`home/StarterPlayerScripts/StarterPlayerScripts/EntranceLeaveController.client.luau`（新增）、`项目设定相关/架构与Remote登记.md`、`项目设定相关/基础技术框架.md`（第7.2节路径表补充 StarterPlayerScripts 一行）、`项目设定相关/策划案.md`（用户自行补充 V1.1.1）
- 遗留问题（下次接手前需要做）：
  1. **用户需要在 Studio 里对家园 Place 的 `StarterPlayerScripts` 也执行一次 "Sync to..."**，绑定到本地 `home/StarterPlayerScripts` 目录，否则新的客户端脚本不会被 Studio 识别——这一步文档已写但未确认用户已完成。
  2. 尚未做任何 Playtest 验证。需要验证的点：两个不同入口能否同时被两个玩家独立占用且互不干扰；入口被占用后第三个玩家是否真的被挡板挡住进不去；中途走出区域和点击 Leave 按钮是否都能正确释放并恢复 Tips 文案颜色；碰撞组切换是否有明显的物理穿模/卡顿。
  3. 已知不处理的边缘情况（设计阶段就明确接受，不是遗漏）：同一帧内两个玩家踏入同一空闲入口的极端并发只有先遍历到的会被判定占用；占用期间玩家中途换装新增的 BasePart 不会被追加设置碰撞组（5秒窗口内风险低）。
- 验证情况：两个新脚本均用本地 `lua -e "loadfile(...)"` 做过语法检查，能通过 Lua 解析；Luau/Roblox API 专属语法（如 `PhysicsService`/`CollisionGroup`/`PivotTo`）和运行时行为完全未经 Studio 实测，是按官方 API 文档推断写的。
- Git 提交：未提交

## 2026-07-07 修复：改名去掉 .legacy 后缀导致脚本变成 ModuleScript 不执行

- 改了什么：用户反馈两个 Place 都已发布，但线上进入 `Entrance_Solo` 范围完全没有传送，也没有任何日志。排查后发现是我自己在上一条记录里犯的错——把旧文件 `RoundEntranceService.legacy.luau` 重写时顺手改名成了 `RoundEntranceService.luau`，去掉了 `.legacy` 后缀。Script Sync（跟 Rojo 约定一致）靠**文件名后缀**决定同步成什么实例类型：`.server.luau`→Script(Server)，`.client.luau`→LocalScript，`.legacy.luau`→Script(Legacy)，纯 `.luau`（无这些后缀）→**ModuleScript**。去掉 `.legacy` 后缀后这个脚本被同步成了 ModuleScript，而 ModuleScript 只有被其他脚本 `require()` 才会执行，项目里没有任何地方 require 它，所以它自始至终都没跑过——这解释了"没有传送也没有任何日志"，因为代码根本没执行，不是传送逻辑本身的问题。修复：文件改名为 `RoundEntranceService.server.luau`，恢复为服务端脚本。同时在《基础技术框架.md》7.1节补充了完整的文件名后缀对照表，避免以后再犯同样的错。
- 影响文件：`home/ServerScriptService/ServerScriptService/Home/RoundEntranceService.server.luau`（从 `RoundEntranceService.luau` 改名，内容不变）、`项目设定相关/基础技术框架.md`（7.1节补充后缀对照表，7.2节路径示例加回正确后缀）
- 遗留问题（下次接手前需要做）：
  1. 改名后需要用户在 Studio 里确认 Script Sync 已经把对应实例的类型从 ModuleScript 变回 Script(Server)，并重新发布两个 Place，再实际测一次传送效果——目前还是"预期修复"，未经过线上验证。
  2. 之前记录里"遗留问题2/3"（路径存在性没做判断、传送后落地流程没做）依然有效，还没处理。
- 验证情况：文件名后缀→实例类型的映射规则来自 Rojo 官方文档交叉确认（Studio Script Sync 采用相同约定），本地已用 `find`/`git status` 确认改名生效；线上传送效果待用户重新发布后验证。
- Git 提交：未提交

## 2026-07-07 落地 V1.1 场景切换，修正 Script Sync 实际目录结构

- 改了什么：用户确认已在 Studio 里对家园/对局两个 Place 完成 Script Sync 的 "Sync to..." 绑定。检查本地实际生成的目录，发现两点跟上一条记录里写的文档不符：(1) Script Sync 对一个实例执行 Sync to 时会在目标目录下再自动生成一层同名子文件夹（比如 `home/ServerScriptService` 下实际是 `home/ServerScriptService/ServerScriptService/...`），不是配置错误；(2) 落地的脚本文件扩展名是 `.luau` 不是 `.lua`。已更新《基础技术框架.md》第7.2/7.3节的路径示例和说明。同时在 `home/ServerScriptService/ServerScriptService/Home/` 下发现一个已存在的脚本 `RoundEntranceService.legacy.luau`，内容是完整的单人/双人/四人局入口停留5秒传送逻辑——用户确认这是之前的草稿，要求参考它重新实现并删除旧文件；同时策划案《策划案.md》V1.1 明确只要求单人局，用户确认收窄范围，只做 `Entrance_Solo`。重写为 `RoundEntranceService.luau`：`RunService.Heartbeat` 轮询检测玩家是否在 `workspace.Base.RoundEntrances.Entrance_Solo` 的 Part 范围内（用 `CFrame:PointToObjectSpace` 做局部坐标包围盒判断），连续停留满5秒（`os.clock()` 计时）后 `TeleportService:TeleportAsync` 传到 `battle` Place（`BATTLE_PLACE_ID = 78013815855891`），传送中标记 `teleporting` 防止重复触发，传送失败会 `warn` 并回滚标记允许重试，玩家离开时清理计时状态。
- 影响文件：`home/ServerScriptService/ServerScriptService/Home/RoundEntranceService.luau`（新写，替换同目录下的 `.legacy.luau` 旧文件）、`项目设定相关/基础技术框架.md`（第7.2/7.3节路径修正）、`README.md`（当前阶段更新）
- 遗留问题（下次接手前需要做）：
  1. **尚未在 Studio 里做 Playtest 验证**——没有实测确认玩家进入 `Entrance_Solo` 范围5秒后是否真的触发传送，逻辑是按策划案文字描述和 Roblox API 推断写的，不是已验证的事实。
  2. `workspace.Base.RoundEntrances.Entrance_Solo` 这个路径引用没有做存在性判断，如果 Studio 里实际路径命名有出入，脚本会直接报错，需要 Playtest 时留意报错信息。
  3. 传送到 `battle` Place 后玩家落地的位置/后续流程完全没做（策划案本来就说"其他都不用做"），下次要扩展时再补。
- 验证情况：目录结构和文件扩展名是本地 `find`/`ls` 直接观察到的事实，已验证；`RoundEntranceService.luau` 的语法用本地 `lua` 解释器做过 `loadfile` 语法检查（能通过 Lua 解析，但 Luau 语法和 Roblox API 行为未经 Studio 实测，仅为推断）；传送逻辑本身的运行时正确性未验证。
- Git 提交：未提交

## 2026-07-07 确定用 Script Sync 做版本管理，建本地代码目录骨架

- 改了什么：用户在 Studio 里开启了官方 Script Sync 功能，确定项目用这个做代码本地化和版本管理，不引入 Rojo。查阅官方文档/DevForum 确认 Script Sync 只同步 `Script`/`LocalScript`/`ModuleScript`/`Folder` 四种实例（双向同步到本地 `.lua`），定位是"Studio 为准"（不接管场景搭建，跟 Rojo 的"文件系统为准"不同），且同一文件夹里混脚本和非脚本实例时非脚本部分会被忽略。据此确定目录映射：仓库根目录建 `home/`（对应家园 Place）和 `battle/`（对应对局 Place）两个顶层目录，各自内部按 `ServerScriptService`/`ReplicatedStorage/Modules`/`StarterPlayerScripts` 分层，`Remotes`/`ItemModels` 等非脚本内容不纳入同步。已建好空目录骨架（用 `.gitkeep` 占位），Studio 里的实际 Sync to 绑定还没做。《基础技术框架.md》第7节原来"建议接入 Rojo"的内容改写为 Script Sync 的完整说明（是什么、跟 Rojo 的区别、目录映射、已知限制、操作步骤）。
- 影响文件：`项目设定相关/基础技术框架.md`（第7节重写）、`README.md`（新增"本地代码目录"小节）、`home/`、`battle/`（新建目录骨架）
- 遗留问题（下次接手前需要做）：
  1. 还没有在 Studio 里对两个 Place 实际执行 "Sync to..." 绑定，文档写的是操作步骤，不是已验证的结果。
  2. 两个 Place 是独立 DataModel，`ItemDefinitions.lua`/`LootTable.lua` 这类可能两边都要用的数据模块目前没有共享机制，只能手动维护两份保持一致，已在文档里记录为已知限制，暂不处理。
- 验证情况：Script Sync 的行为和限制来自官方 DevForum 公告（[Full Release 帖](https://devforum.roblox.com/t/full-release-studio-script-sync/4688454)）和网页搜索交叉确认，非本地实测；本地目录骨架已用 `ls`/`find` 确认创建成功。
- Git 提交：未提交

## 2026-07-03 对局 Place 改名为 battle

- 改了什么：用户在 Studio 游戏设置里把对局 Place 的名字从默认 `Place1` 改成 `battle`，后台截图确认改名生效。MCP 查询该 Place 时因连接的是家园 Place 会话未能直接复核，但后台显示已确认为准，README 里对局 Place 的 placeName 更新为 `battle`。
- 影响文件：`README.md`
- 验证情况：用户提供的后台管理页截图确认改名成功；MCP `get_place_info` 未能直接复核（当时连接的会话是家园 Place），不影响结论采信。
- Git 提交：未提交

## 2026-07-03 新建对局 Place，绑定信息写入 README

- 改了什么：用户在 Studio 里手动新建了对局 Place（提前于阶段8做，用户主动选择现在就建，不走"临时区域验证手感"方案），确认与家园 Place 同属一个 Universe（gameId 均为 `9971501998`）。当前对局 Place 内容仍是 Studio 默认模板（`Baseplate`/`SpawnLocation`/`Terrain`/`Camera`），未清空搭建。placeName 还是默认的 `Place1`，待改成更明确的名字区分两个 Place。README.md 的 Studio 绑定信息区块改为家园/对局两个 Place 分别列出，并加了"操作前先用 get_place_info 确认连的是哪个 Place"的提醒。
- 影响文件：`README.md`
- 验证情况：`get_place_info` 确认新 Place 的 placeId `78013815855891`、gameId `9971501998`。清空模板场景尚未做，未验证。
- Git 提交：未提交

## 2026-07-03 定下开发顺序规划，分 10 阶段推进

- 改了什么：确定项目正式开发的顺序——新建《开发顺序规划.md》，分 10 个阶段（1.物品与掉落数据地基 → 2.单人局核心循环最小可玩版 → 3.局内经济成长 → 4.结算与解锁判定 → 5.局外持久化 → 6.家园Plot与展示柜 → 7.多人局扩展 → 8.双Place正式分离 → 9.性能与体验打磨 → 10.数值平衡与内容扩充），每阶段配套"策划产出"和"技术产出"，策划案按这个顺序依次输出。阶段 2（核心循环）是全项目风险最高的验证点，优先做最小可玩版而不是先补外围机制。更新 README.md 文档索引和当前阶段说明。
- 影响文件：`项目设定相关/开发顺序规划.md`（新建）、`README.md`
- 验证情况：文档任务，纯规划内容，未涉及代码或 Studio。
- Git 提交：未提交

## 2026-07-02 Studio 场景改为手动搭建，废弃此前 AI 拼的骨架

- 改了什么：之前几条记录里 AI 拼的 `Workspace.Home` / `Plots`（8个Plot+Shelf+SpawnPoint）骨架不满意，已被用户手动删除，改为用户自己手动重新搭建。**以下为当前 Studio 实际结构，此前几条关于场景骨架的记录（"补充Plot分配机制设定"、"搭建家园最小骨架"）中涉及具体节点命名/数量/Tag 的部分已过时，仅保留其中仍适用的设计思路（如"固定槎位+运行时动态分配"的机制本身没有被否定，只是命名和落地方式变了）**。
  - `Workspace.Base.RoundEntrances`：`Entrance_Solo` / `Entrance_Duo` / `Entrance_Squad`（Part），Tag `RoundEntrance`，Attribute `MaxPlayers`（1/2/4）
  - `Workspace.Base.Home01` ~ `Home15`：15 个家园地块（Folder），每个含 `Floor`（Part）+ `SpawnLocation`（含 Decal），目前**没有打 Tag/Attribute**，也**没有展示柜（Shelf）**
  - `Workspace.Base.Ground`：地面（Part，无 Tag）
  - `ServerScriptService`/`ServerStorage`/`ReplicatedStorage` 均为空，纯场景骨架，无任何脚本
- 影响文件：`开发记录.md`（本条）；Studio 场景（用户手动操作，未通过 MCP 改动）
- 遗留问题（下次接手/开工前需要决定）：
  1. Home01~15 目前没有 Tag/Attribute，《基础技术框架.md》1.1 节设计的 Plot 分配逻辑（`CollectionService:GetTagged("Plot")`、`Occupied`/`PlotIndex` Attribute）要落地的话，需要给这 15 个 Home 补上对应 Tag/Attribute，或者调整设计文档匹配现在的命名（`Home01`~`Home15` 而不是 `Plot_N`）。
  2. 当前 Home 地块下没有展示柜模型，"局外收藏解锁点亮展示柜"这个核心玩法的呈现载体还没有物理对象，需要后续补上。
- 验证情况：`get_project_structure`/`get_tags`/`get_attributes` 直接查询 Studio 当前状态确认。
- Git 提交：未提交

## 2026-07-02 补充 Plot 分配机制设定，Studio 骨架改为多 Plot 结构（⚠️ 已过时，见上条）

- 改了什么：补充设定——家园里每个玩家有专属的出生点+展示柜，通过固定数量 Plot 槎位+运行时动态分配实现（不随玩家数增长场景内容，归属关系存内存不落库）。更新《基础技术框架.md》1.1/3.3/4.1/6节和小结。Studio 内删除原共享 `DisplayShelves`，新建 `Workspace.Home.Plots`，环形摆放 8 个 Plot（Tag: `Plot`，Attribute: `PlotIndex`/`Occupied`），每个 Plot 含 `Shelf`（Tag: `PlotShelf`）和 `SpawnPoint`（Tag: `PlotSpawn`）；共享中立出生点 `SpawnLocation` 挪到圆心，等待后续服务端脚本分配 Plot 后传送玩家。
- 影响文件：`项目设定相关/基础技术框架.md`；Studio 内 `Workspace.Home.Plots`（新建8个）、`Workspace.Home.DisplayShelves`（删除）、`Workspace.SpawnLocation`（位置调整）
- 验证情况：`get_project_structure` 确认 8 个 Plot 及其 Shelf/SpawnPoint 子结构均已生成；未做 Playtest（Plot 分配逻辑代码还没写，只是场景骨架）。
- Git 提交：未提交

## 2026-07-02 搭建家园最小骨架（地面/展示柜/开局入口占位）（⚠️ 已过时，场景已被用户删除重搭，见上方最新记录）

- 改了什么：在 `Workspace.Home` 下搭了最小可用骨架——删除默认 Baseplate，新建 120x120 地面；`DisplayShelves` 下 3 个展示柜占位（Tag: `DisplayShelf`，Attribute: `SlotIndex`）；`RoundEntrances` 下 1/2/4 人局传送点占位（Tag: `RoundEntrance`，Attribute: `MaxPlayers`）；调整 SpawnLocation 位置让玩家出生即可见。全部是占位方块，走 Tag+Attribute 而非命名查找，方便后续脚本用 `CollectionService:GetTagged()` 接逻辑。
- 影响文件：Studio 内 `Workspace.Home`（新建）、`Workspace.SpawnLocation`（位置调整）；无本地文件改动
- 验证情况：`get_project_structure` 确认层级和子对象数量正确（3 shelves + 3 entrances）。未做 Playtest（还没有交互逻辑可测）。
- Git 提交：未提交（本次是 Studio 内容改动，不产生本地文件 diff，后续如导出场景快照再考虑要不要纳入 git）

## 2026-07-02 接入 Studio，清空模板场景，绑定家园 Place

- 改了什么：连接当前 Roblox Studio 会话（`Place1`），确认为通用模拟器模板底子（GroceryStore/TeleportPads/Leaderboards 等），与项目无关，已删除 `Workspace.LobbyScene` 清空场景。确定 `Place1` 角色为家园 Place，绑定信息写入 README.md。对局 Place 待发布游戏后新建。
- 影响文件：`README.md`；Studio 内 `Workspace.LobbyScene`（已删除）
- 验证情况：`get_project_structure`/`get_instance_children` 确认删除后 Workspace 只剩默认的 Terrain/Baseplate/SpawnLocation/Camera。
- Git 提交：未提交

## 2026-07-02 项目文档基线首次提交

- 改了什么：git init 并完成首次提交，把当时已有的全部设计文档（核心玩法循环、基础技术框架、精简版工作流入口、架构与Remote登记）一并纳入版本管理。仓库用户名设为 `luyucun`（仅本仓库生效，未加 --global）。
- 影响文件：全部当前文件（README.md、开发记录.md、项目设定相关/*、核心体验参考图/*）
- 验证情况：`git log` 确认提交已生成，工作区干净。
- Git 提交：`0521196 docs: 初始化项目文档基线`

## 2026-07-02 精简项目工作流文档结构

- 改了什么：参考旧项目的开发流程模板，去掉过重的部分（多份文档强制全读、绑定旧机器路径的健康检查脚本），精简成 `README.md` + `开发记录.md` + `架构与Remote登记.md` 三件套，新对话开工只需读入口文件、最近几条记录和 git status。
- 影响文件：`README.md`（新增）、`开发记录.md`（新增）、`项目设定相关/架构与Remote登记.md`（新增）
- 验证情况：文档任务，已复查内容和路径引用，未涉及代码或 Studio。
- Git 提交：`0521196 docs: 初始化项目文档基线`

## 2026-07-02 基础垃圾拾取方案补充

- 改了什么：在《基础技术框架.md》第 2 节补充"基础垃圾拾取"专项方案（2.1 小节）——客户端预测+服务端裁决、防双计数标记、节流打包发送、CollectionService 打 Tag、对象池复用。
- 影响文件：`项目设定相关/基础技术框架.md`
- 验证情况：文档任务，已复查改动内容，未涉及代码。
- Git 提交：`0521196 docs: 初始化项目文档基线`

## 2026-07-02 基础技术框架定稿

- 改了什么：输出场景架构（家园/对局分 Place）、服务端客户端分工表、数据存储分层（局内内存/局外 DataStore）、资源加载与性能优化、Remote 组织规范、建议文件夹结构。调用了 `roblox-development` 技能的 patterns/sharp_edges/validations 参考文件。
- 影响文件：`项目设定相关/基础技术框架.md`
- 验证情况：文档任务，未涉及代码或 Studio（Studio 插件当时未连接）。
- Git 提交：`0521196 docs: 初始化项目文档基线`

## 2026-07-02 核心玩法循环定稿

- 改了什么：基于用户提供的核心体验参考图和口头描述，输出局内循环（翻找→掉落→变现/成长→结算）和局外循环（家园展示→缺口驱动→开新局）的双循环玩法文档。
- 影响文件：`项目设定相关/核心玩法循环.md`
- 验证情况：文档任务，纯设计内容。
- Git 提交：`0521196 docs: 初始化项目文档基线`
